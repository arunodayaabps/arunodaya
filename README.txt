ARUNODAYA ABPS — Aesthetic Science Education Website

Student flow:
Home -> Class 6/7/8 -> Science -> Chapter -> View Material

The design uses an original inline science illustration and responsive CSS, so the site does not depend on stock-image URLs.

Current prototype upload:
admin.html stores files in browser localStorage.

For production:
- secure teacher/admin login
- cloud/server document storage
- database for chapters/material metadata
- protected document viewer
- PDF-to-page/image rendering if stronger view-only protection is required

Important: no normal website can guarantee that a determined user cannot copy/screenshot content. The final protected viewer should avoid exposing the original file URL.
